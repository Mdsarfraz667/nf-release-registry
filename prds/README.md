# PRD PDFs

Human-readable source PDFs for registry bundles, uploaded by
`scripts/new-bundle.sh --pdf …` as `prds/<BUNDLE-ID>.pdf`.

The QA agent does **not** read these files — it uses the plain text under
`## PRD / QA requirements` on the matching GitHub issue (`qa_intent`).
